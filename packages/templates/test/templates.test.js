'use strict';

const templates = require('..');

describe('templates', () => {
  it('should compile pug');
  it('should compile handlebar');

  it('should compile pug with lang');
  it('should compile handlebar with lang');

  it('should compile pug with lang detected');
  it('should compile handlebar with lang detected');
});
