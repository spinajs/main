'use strict';

const jobs = require('../lib/jobs');

describe('jobs', () => {
  it('should load jobs');
  it('should exeute jobs once');
  it('should emit event locally');
  it('should subscribe multiple times');
});
