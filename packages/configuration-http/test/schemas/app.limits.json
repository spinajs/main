{
  "$id": "app.limits",
  "type": "object",
  "properties": {
    "perPage": { "type": "integer", "minimum": 1, "maximum": 100 },
    "export": { "type": "boolean" }
  },
  "required": ["perPage"],
  "additionalProperties": false
}
